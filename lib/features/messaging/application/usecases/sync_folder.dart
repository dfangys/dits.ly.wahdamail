import '../../domain/entities/folder.dart';

/// Use case: SyncFolder (skeleton)
class SyncFolder {
  const SyncFolder();

  Future<void> call(Folder folder) async {
    // Skeleton orchestration; actual infra sync is out of scope for P1
    await Future.value();
  }
}

